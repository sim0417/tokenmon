# 기여 안내

tokenmon에 관심 가져주셔서 감사합니다. 작은 개인 프로젝트라 규칙은 최소한으로만 두었습니다.

## 개발 환경

- macOS (트레이 아이콘과 Keychain 연동이 macOS 전용입니다)
- Node.js 20 이상

```bash
npm install
npm start    # 앱 실행
npm run lint # 코드 검사
npm test     # 테스트 (31개)
```

## 보내주시기 전에

- `npm run lint`와 `npm test`가 모두 통과하는지 확인해주세요. CI에서도 동일하게 검사합니다.
- 로직을 추가하거나 고치셨다면 `test/`에 테스트를 함께 넣어주세요. 외부 네트워크나 Electron에 의존하지 않는 순수 함수 단위로 작성하시면 됩니다.

## 커밋 메시지

`타입: 한국어 존댓말 평서문` 형태로 적습니다. 타입으로 성격을 분류하고, 내용은 우리말로 씁니다.

```
feat: 사용량 패널을 링 형태로 새로 만듭니다
fix: 드래그 후 펫이 엉뚱한 위치로 이동하는 문제를 고칩니다
docs: 설계 문서를 docs/design.md 하나로 정리합니다
```

### 타입

| 타입 | 언제 |
|---|---|
| `feat` | 사용자가 체감하는 기능이 새로 생겼을 때 |
| `fix` | 의도대로 동작하지 않던 것을 고쳤을 때 |
| `refactor` | 동작은 그대로 두고 구조만 바꿨을 때 |
| `perf` | 성능이나 자원 사용을 개선했을 때 |
| `style` | 코드 포맷이나 UI 스타일만 손봤을 때 |
| `docs` | 문서만 바꿨을 때 |
| `test` | 테스트를 추가하거나 고쳤을 때 |
| `chore` | 빌드, CI, 의존성, 저장소 설정 등 |

`fix`와 `refactor`가 헷갈릴 때는 **그 변경이 없었다면 사용자가 무언가 잘못된 것을 봤을지**로 가릅니다. 봤다면 `fix`, 아니면 `refactor`입니다.

### 범위 (선택)

건드린 곳이 뚜렷하면 괄호로 덧붙입니다. 여러 곳에 걸치거나 애매하면 생략하세요.

```
fix(usage): 소스를 바꿔도 이전 LLM의 수치가 남던 문제를 고칩니다
feat(panel): 사용량 패널을 링 형태로 새로 만듭니다
```

주로 쓰는 범위는 `pet`, `panel`, `bubble`, `dex`, `tray`, `usage`, `config`, `ci`입니다. 저장소의 `area:` 라벨도 같은 이름을 쓰므로, 이슈에 붙은 라벨을 보면 어떤 범위로 커밋할지 짐작하실 수 있습니다.

### 제목

타입을 뺀 본문이 `~합니다`로 끝나는 한 문장이 되게, 40자 내외를 권합니다. 마침표는 붙이지 않습니다.

### 본문

빈 줄을 하나 두고 이어 씁니다. 무엇을 바꿨는지는 diff를 보면 알 수 있으니, **왜 그 변경이 필요했는지와 코드만 봐서는 알 수 없는 배경**을 적어주세요. 갈래가 여럿이면 `-`로 나열합니다. 한 줄로 충분한 변경이라면 본문 없이 제목만 있어도 괜찮습니다.

```
fix(usage): 소스를 바꾸면 이전 LLM의 사용량이 남던 문제를 고칩니다

설정에서 Claude와 Codex를 오가도 새로고침을 누르기 전까지 이전 소스의
수치가 그대로 보였습니다. 설정 변경을 받으면 config만 다시 읽고 화면을
그릴 뿐, 다시 조회하지 않았기 때문입니다.

- 캐시를 소스별로 나눠 담습니다.
- 소스가 바뀌면 화면 값을 비우고 곧바로 다시 조회합니다.
```

### 템플릿

저장소에 `.gitmessage`를 두었습니다. 아래 명령을 한 번 실행해두면 `git commit` 때 안내가 함께 뜹니다.

```bash
git config commit.template .gitmessage
```

이 규칙은 이번에 정한 것이라, 그 이전 커밋에는 타입이 붙어 있지 않습니다.

## 이 프로젝트의 방침

몇 가지는 의도적인 선택이라 미리 알려드립니다.

- **의존성을 늘리지 않습니다.** 런타임 의존성은 Electron 하나뿐이고, 나머지는 Node 내장 모듈로 해결하고 있습니다. 새 패키지가 꼭 필요하다면 이슈에서 먼저 이야기를 나눠주세요.
- **렌더러의 `nodeIntegration`이 켜져 있습니다.** 외부 콘텐츠를 띄우지 않는 로컬 전용 앱이라 내린 결정입니다. 다만 사용자 입력이나 외부에서 들어온 문자열을 DOM에 넣을 때는 반드시 이스케이프해주세요.
- **스프라이트 이미지는 저장소에 넣지 않습니다.** 실행 중에 내려받아 로컬에만 둡니다.

## 큰 변경을 계획 중이시라면

작업을 시작하시기 전에 [이슈](https://github.com/Kimsoo0119/tokenmon/issues)로 먼저 제안해주세요. 방향이 어긋난 채로 시간을 쓰시는 일이 없었으면 합니다.
